{
  "name": "pnpm-audit-hook",
  "version": "1.0.0",
  "description": "Enterprise pnpm hook system for pre-install security auditing and Azure DevOps integration.",
  "license": "MIT",
  "type": "commonjs",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "bin": {
    "pnpm-audit-hook": "dist/src/cli.js"
  },
  "files": [
    "dist",
    ".pnpmfile.cjs",
    ".pnpm-audit.schema.json",
    ".pnpm-audit.yaml",
    "azure-pipelines",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "test:ts": "node --test test/*.test.ts",
    "prepare": "npm run build"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "semver": "^7.6.3",
    "yaml": "^2.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/semver": "^7.5.8",
    "typescript": "^5.9.3"
  }
}
