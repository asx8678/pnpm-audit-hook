# Example release gate (maximum strictness)
trigger: none

stages:
  - stage: ReleaseGate
    jobs:
      - template: ../templates/pnpm-audit-job.yml
        parameters:
          severityThreshold: 'low'
          failOnWarn: true
          reportFormat: 'sarif,html,json,junit,markdown,sbom-cyclonedx'
